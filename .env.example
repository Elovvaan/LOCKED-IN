PORT=3000
HOST=0.0.0.0
# REQUIRED in production (Railway): set a strong random value via the Railway dashboard
JWT_SECRET=your-secret-key-here
NODE_ENV=development
# SEED_PASSWORD=Seed1234!
# Optional: restrict CORS to a specific origin (leave unset to allow all origins)
# CORS_ORIGIN=https://your-app.up.railway.app
