# For physical device or emulator, replace with your machine's LAN IP (e.g. 192.168.1.x).
# Do NOT use localhost — it will not reach the backend from a real device.
EXPO_PUBLIC_API_URL=http://192.168.1.x:3000
